name: CI

on:
  push:
    paths:
      - 'nestjs-go-export-excel-wasm/**'
      - '.github/workflows/nestjs-go-export-excel-wasm-ci.yml'
  pull_request:
    paths:
      - 'nestjs-go-export-excel-wasm/**'
      - '.github/workflows/nestjs-go-export-excel-wasm-ci.yml'

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: nestjs-go-export-excel-wasm
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.10

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate Prisma client
        run: bun run prisma:generate

      - name: Format check
        run: bun run format:check

      - name: Lint
        run: bun run lint

      - name: Build Nest app
        run: bun run build

  go-wasm:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: nestjs-go-export-excel-wasm
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.10

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: nestjs-go-export-excel-wasm/excel-streamer/go.mod

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build Go WASM bridge
        run: bun run build:wasm

      - name: Upload Go WASM artifacts
        uses: actions/upload-artifact@v4
        with:
          name: go-wasm-artifacts
          path: |
            nestjs-go-export-excel-wasm/excel-streamer/excel_bridge.wasm
            nestjs-go-export-excel-wasm/excel-streamer/wasm_exec.js

  rust-wasm:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: nestjs-go-export-excel-wasm
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.10

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build Rust WASM bridge
        run: bun run build:rust-wasm

      - name: Smoke test Rust WASM bridge
        run: bun run test:rust-wasm

      - name: Upload Rust WASM artifacts
        uses: actions/upload-artifact@v4
        with:
          name: rust-wasm-artifacts
          path: |
            nestjs-go-export-excel-wasm/rust-excel-streamer/pkg

  unit-tests:
    runs-on: ubuntu-latest
    needs:
      - lint-and-build
      - rust-wasm
    defaults:
      run:
        working-directory: nestjs-go-export-excel-wasm
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.10

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate Prisma client
        run: bun run prisma:generate

      - name: Download Rust WASM artifacts
        uses: actions/download-artifact@v4
        with:
          name: rust-wasm-artifacts
          path: nestjs-go-export-excel-wasm/rust-excel-streamer/pkg

      - name: Run unit tests
        run: bun run test

  e2e-and-comparison:
    runs-on: ubuntu-latest
    needs:
      - lint-and-build
      - go-wasm
      - rust-wasm
    defaults:
      run:
        working-directory: nestjs-go-export-excel-wasm
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.10

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate Prisma client
        run: bun run prisma:generate

      - name: Download Go WASM artifacts
        uses: actions/download-artifact@v4
        with:
          name: go-wasm-artifacts
          path: nestjs-go-export-excel-wasm/excel-streamer

      - name: Download Rust WASM artifacts
        uses: actions/download-artifact@v4
        with:
          name: rust-wasm-artifacts
          path: nestjs-go-export-excel-wasm/rust-excel-streamer/pkg

      - name: Build Nest app
        run: bun run build

      - name: Run e2e tests
        run: bun run test:e2e

      - name: Run comparison validation
        run: |
          PORT=3100 bun dist/src/main.js &
          APP_PID=$!
          trap 'kill $APP_PID' EXIT
          for i in {1..30}; do
            curl -sf http://localhost:3100/export/exceljs/health >/dev/null && break
            sleep 1
          done
          BASE_URL=http://localhost:3100 bun run test:comparison
